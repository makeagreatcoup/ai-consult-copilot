# macOS 依赖清单（智谱 GLM 版）
# 安装前先：brew install portaudio ffmpeg
# 然后：pip install -r requirements-mac.txt

fastapi>=0.104.0
uvicorn>=0.24.0
pyaudio>=0.2.14
numpy>=1.24.0
faster-whisper>=1.0.0
zhipuai>=2.1.0
sniffio>=1.3.0             # zhipuai 的隐式依赖（需显式声明）
websockets>=12.0
python-dotenv>=1.0.0
watchdog>=3.0.0
